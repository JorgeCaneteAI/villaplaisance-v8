# 2026-04-30 — Setup V8, Phase 0

## Décisions structurantes

1. **Garder le back-office et la DB intacts** (Jorge a confirmé que
   `https://villaplaisance.fr/admin/dashboard` lui convient parfaitement).
2. **Refondre uniquement le front** : Views Front + assets, en portant
   le design du dossier `villaplaisance-impeccable-V2/`.
3. **Repo dédié `villaplaisance-v8`** (à créer sur GitHub), isolé du
   repo `villaplaisance-v7` qui continue à servir la prod.
4. **Sous-domaine `v2.villaplaisance.fr`** sur o2switch pour valider
   en condition réelle, pendant que la prod reste sur `villaplaisance.fr`.
5. **DB séparée `efkz3012_VPV8`** sur o2switch, copie de la prod, mais
   distincte. Aucune écriture sur la prod tant que la bascule n'est pas faite.
6. **Bascule finale** : quand le V8 est validé, on remplace le code de
   `villaplaisance.fr` par celui du V8, on switch la DB, et on archive le V7.

## État après Phase 0

- Dossier local créé : `~/Documents/C.L.A.U.D.E/villaplaisance-v8/`
- Code copié depuis le snapshot du 27 avril (`villa-plaisance-2026-04-27/code/`).
- `.env` local créé avec credentials MAMP par défaut (`localhost:8889`, `root/root`).
- `.env.example` mis à jour avec `DB_PORT`.
- `Database.php` patché pour supporter `DB_PORT` (rétrocompatible : default = `3306`).
- `.gitignore` créé (exclut `.env`, `public/uploads/`, dump SQL, etc.).
- `git init` fait, premier commit prêt à être créé.

## Phase 0 — TERMINÉE le 2026-05-01

- [x] DB locale `villaplaisance_v8_dev` créée dans MAMP MySQL 8.0.44.
- [x] Dump importé (28 tables).
- [x] Serveur PHP local lancé sur `http://localhost:8767/`.
- [x] Home affichée correctement avec l'ancien design v7.

### Pièges MariaDB → MySQL rencontrés (à retenir)

Le dump `database/vp_v7.sql` est généré sur **MariaDB** (sur o2switch) et
contient deux incompatibilités avec MySQL 8 :

1. **Collation `utf8mb4_uca1400_ai_ci`** (spécifique MariaDB 10.10+, inconnue
   de MySQL). Solution : créer un dump dérivé avec `utf8mb4_unicode_ci`
   (compatible des deux côtés) :
   ```bash
   sed 's/utf8mb4_uca1400_ai_ci/utf8mb4_unicode_ci/g' \
     database/vp_v7.sql > database/vp_v7-mysql.sql
   ```

2. **Foreign keys avec noms en doublon** (`CONSTRAINT \`1\``). MariaDB
   tolère des FK avec le même nom dans deux tables différentes du même
   schéma, MySQL refuse. Solution : renommer la 2e occurrence
   (`fk_itinerary_steps_itinerary`).

Le dump `vp_v7-mysql.sql` est gitignoré (généré à la demande, pas de
duplication dans le repo).

### Lancement local — commande définitive

Le serveur PHP built-in route TOUTES les requêtes vers `index.php` quand
on lui passe ce fichier en router. Du coup les fichiers statiques
(CSS, JS, images) ne sont jamais servis. Solution : un `router.php` à
la racine qui retourne `false` pour les fichiers existants et délègue
le reste à `public/index.php`. Lancer avec `-t public` (DocRoot =
`public/`) pour que `return false` serve le fichier depuis le bon dossier.

```bash
cd ~/Documents/C.L.A.U.D.E/villaplaisance-v8
php -S localhost:8767 -t public router.php
```

DB :
```bash
MYSQL=/Applications/MAMP/Library/bin/mysql80/bin/mysql
$MYSQL -u root -proot -h 127.0.0.1 -P 8889 villaplaisance_v8_dev
```

## Phase 1 — TERMINÉE le 2026-05-01

- [x] Repo GitHub `JorgeCaneteAI/villaplaisance-v8` créé (public).
- [x] Premier push avec 4 commits (init, router, .cpanel.yml, .htaccess).

## Phase 2 — TERMINÉE le 2026-05-01

- [x] Sous-domaine `v2.villaplaisance.fr` créé via cPanel
      (DocumentRoot : `/home/efkz3012/v2.villaplaisance.fr/public`).
- [x] DB o2switch `efkz3012_VPV8` créée + user `efkz3012_VPV8` avec
      tous privilèges. Password noté chez Jorge.
- [x] cPanel Git Version Control : repo cloné dans
      `/home/efkz3012/repositories/villaplaisance-v8`.
- [x] Premier déploiement déclenché manuellement
      (`cp -R * $DEPLOYPATH`).
- [x] `.env` de prod créé dans `/home/efkz3012/v2.villaplaisance.fr/.env`,
      permissions `600`.
- [x] DB importée depuis la prod via `mysqldump | mysql` direct
      (28 tables transférées, MariaDB → MariaDB sans souci).
- [x] Uploads copiés depuis la prod (46 MB, 172 fichiers).
- [x] AutoSSL Let's Encrypt activé sur le sous-domaine.
- [x] `https://v2.villaplaisance.fr/` répond `HTTP/2 200`.

### Piège rencontré : `.htaccess` écrasé par le `.cpanel.yml`

Le `.cpanel.yml` initial faisait `cp .htaccess $DEPLOYPATH/public/.htaccess`,
ce qui écrasait le bon `public/.htaccess` (1150 bytes, contient PWA + Force
HTTPS + routing) avec le mini `.htaccess` racine (125 bytes, juste un redirect
interne vers `/public/`).

Le mini `.htaccess` racine sert uniquement quand le DocumentRoot Apache est à
la racine du projet ; ici le sous-domaine pointe directement sur `public/`,
donc on ne le copie pas. Ligne supprimée du `.cpanel.yml`.

### Workflow de déploiement à partir de maintenant

```
[local] git push origin main
   ↓
[GitHub] webhook
   ↓
[cPanel Git] détecte le nouveau commit
   ↓
[.cpanel.yml] cp -R * → /home/efkz3012/v2.villaplaisance.fr/
   ↓
[Apache] sert https://v2.villaplaisance.fr/
```

Note : si cPanel ne déclenche pas auto, déclencher manuellement via
**Git Version Control > Manage > Update from Remote** puis
**Pull or Deploy > Deploy HEAD Commit**.

## Phases suivantes (récap)

- **Phase 1** : créer repo GitHub `villaplaisance-v8`, premier push.
- **Phase 2** : sous-domaine + DB + clone sur o2switch, premier déploiement.
- **Phase 3+** : portage du design V2 dans les Views Front, page par page.

## Stratégie de portage (Phase 3+)

Ordre proposé :
1. **Layout** `Views/layouts/front.php` (header, footer, overlay menu, fontes).
2. **Home** `front/home.php`.
3. **Chambres d'hôtes** `front/chambres.php`.
4. **Villa** `front/villa.php`.
5. **Espaces extérieurs** `front/exterieurs.php`.
6. **Sur place** `front/surplace.php`.
7. **Itinéraire** `front/itinerary.php` (liste + détail + commentaires).
8. **Journal** `front/journal.php` (liste) + `front/article.php` (détail).
9. **Hôte** `front/hote.php`.
10. **Contact** `front/contact.php` (brancher sur `ContactController::submit`).
11. **Légal** : mentions, confidentialité, plan du site, 404.

À chaque page :
- Garder les variables PHP exposées par le Controller (`$seo`, `$lang`, `$pieces`, `$articles`…).
- Remplacer le HTML par le markup V2.
- Adapter le CSS pour ne pas casser l'admin (qui partage le layout `admin.php`, distinct).

## Phase 3 — Avancement

| Étape | Statut | Commit |
|---|---|---|
| **3-A — Fondation V8** (layout, header, footer, overlay, fontes, CSS/JS) | ✅ | `e5fb07a` |
| **3-B — Home V2** (8 actes : opening, manifesto, diptyque, interlude, lieu/mappemonde, voix, journal, FAQ, contact) | ✅ | `704f82c` |
| **3-C — Pages statiques** (mentions, confidentialité, plan-du-site, 404) | ✅ | `31eb670` |
| **3-D — Pages produit** (chambres, villa, exterieurs, sur-place) | ✅ | `a42ab20` |
| **3-E — Pages dynamiques** (journal liste+article, itineraires, votre-hote) | ✅ | `e5a0f5b` |
| **3-F — Contact** (branchement formulaire sur ContactController::submit) | ✅ | `e5a0f5b` |

## Phase 3 — TERMINÉE 🎉

**Tout le portage du design V2 est fait.** Le sous-domaine
`v2.villaplaisance.fr` rend désormais le V2 complet sur les 14 pages
publiques (sauf `/itineraire/{slug}` qui reste sur l'ancien design v7
car c'est une page très spécifique avec carte Google Maps + commentaires
— à porter dans une étape ultérieure si nécessaire).

### Reste à dynamiser (TODO inline dans les Views)

Ces sections ont été portées en STATIQUE depuis le V2 mais devraient
consommer la DB pour rester pilotables depuis l'admin. À adresser dans
une étape ultérieure quand la priorité sera de pouvoir tout modifier
sans toucher au code :

- `home.php` Acte 5 (voix) → `vp_reviews WHERE featured = 1`
- `home.php` Acte 6 (journal home) → `vp_articles ORDER BY published_at DESC LIMIT 3`
- `home.php` Acte 7 (FAQ home) → `vp_faq WHERE page_slug = 'accueil'`
- `home.php` Mappemonde (`main-v8.js`) → `vp_reviews.location`
- `chambres.php` FAQ → `vp_faq WHERE page_slug = 'chambres-d-hotes'`
- `villa.php` FAQ → `vp_faq WHERE page_slug = 'location-villa-provence'`
- `journal.php` liste → `foreach ($articles)` + filtres depuis `$categories`
- `surplace.php` liste → `foreach ($articles)` + filtres depuis `$categories`
- `hote.php` → utiliser `$profile` et `$blocks` de `vp_host_*`

### Pages encore en design v7 (pas portées)

- `/itineraire/{slug}` (détail d'un itinéraire personnalisé pour un guest) :
  page très spécifique avec carte Google Maps embed, frise horaire, formulaire
  de commentaire. Ne fait pas partie du V2 statique. À porter séparément
  si on souhaite l'aligner.
- `/livret` : page protégée par mot de passe, livret d'accueil pour les
  guests. Pas dans le V2 non plus.
- `/admin/*` : tout le back-office, intentionnellement non touché.

### Workflow récapitulatif

```
[local]   git push origin main
   ↓
[GitHub]  commit visible
   ↓
[o2switch SSH cPanel] :
   cd /home/efkz3012/repositories/villaplaisance-v8
   git pull origin main
   export DEPLOYPATH=/home/efkz3012/v2.villaplaisance.fr/
   /bin/cp -R * $DEPLOYPATH
   chmod -R 755 $DEPLOYPATH
   ↓
[Apache] sert https://v2.villaplaisance.fr/
```

### Bascule prod (à faire un jour)

Quand le V8 sera entièrement validé, basculer la prod :
- Option A (la plus propre) : modifier le `.cpanel.yml` du repo
  `villaplaisance-v7` pour pointer vers `v2.villaplaisance.fr/`, et le
  `.cpanel.yml` du repo `villaplaisance-v8` pour pointer vers
  `villaplaisance.fr/`. Puis push sur les deux.
- Option B (plus rapide) : sur o2switch, échanger les sous-dossiers
  `villaplaisance.fr` ↔ `v2.villaplaisance.fr`, et changer le DocumentRoot
  du sous-domaine `v2` vers la prod (et vice-versa).
- Option C (la plus simple à comprendre) : déployer le V8 directement sur
  la prod. C'est-à-dire pointer le `.cpanel.yml` du repo `villaplaisance-v8`
  vers `/home/efkz3012/villaplaisance.fr/`, importer la DB `vp_v8` dans
  `vp_v7` (ou pointer le `.env` de prod vers `vp_v8`), et garder le
  sous-domaine `v2` comme staging permanent.

### Point de reprise — fin de session du 2026-05-04

État actuel :
- Tout le code de l'étape 3-C est sur GitHub (`origin/main` à `31eb670`).
- **Le déploiement de l'étape 3-C n'a pas encore été fait sur o2switch.**
  Au prochain démarrage, lancer en SSH cPanel Terminal :
  ```bash
  cd /home/efkz3012/repositories/villaplaisance-v8
  git pull origin main
  export DEPLOYPATH=/home/efkz3012/v2.villaplaisance.fr/
  /bin/cp -R * $DEPLOYPATH
  chmod -R 755 $DEPLOYPATH
  ```
  Puis tester :
  - https://v2.villaplaisance.fr/mentions-legales/
  - https://v2.villaplaisance.fr/politique-confidentialite/
  - https://v2.villaplaisance.fr/plan-du-site/
  - https://v2.villaplaisance.fr/inexistant (force un 404)

À dynamiser plus tard (TODO inline dans `front/home.php`) :
- Acte 5 (voix) → `vp_reviews WHERE featured = 1`
- Acte 6 (journal home) → `vp_articles ORDER BY published_at DESC LIMIT 3`
- Acte 7 (FAQ home) → `vp_faq WHERE page_slug = 'accueil'`
- Mappemonde (`main-v8.js`) → `vp_reviews.location`

Prochaine session : **étape 3-D (pages produit)**, en commençant par
`chambres-d-hotes` (qui a une structure de référence dans
`villaplaisance-impeccable-V2/chambres-d-hotes/index.html`).

### Comment reprendre vite

```
On reprend Villa Plaisance V8 dans
/Users/jorgecanete/Documents/C.L.A.U.D.E/villaplaisance-v8.
Lis le CLAUDE.md du projet et le dernier doc dans docs/, puis dis-moi
où on en est.
```
