# 2026-04-30 — Setup V8, Phase 0

## Décisions structurantes

1. **Garder le back-office et la DB intacts** (Jorge a confirmé que
   `https://villaplaisance.fr/admin/dashboard` lui convient parfaitement).
2. **Refondre uniquement le front** : Views Front + assets, en portant
   le design du dossier `villaplaisance-impeccable-V2/`.
3. **Repo dédié `villaplaisance-v8`** (à créer sur GitHub), isolé du
   repo `villaplaisance-v7` qui continue à servir la prod.
4. **Sous-domaine `v2.villaplaisance.fr`** sur o2switch pour valider
   en condition réelle, pendant que la prod reste sur `villaplaisance.fr`.
5. **DB séparée `efkz3012_VPV8`** sur o2switch, copie de la prod, mais
   distincte. Aucune écriture sur la prod tant que la bascule n'est pas faite.
6. **Bascule finale** : quand le V8 est validé, on remplace le code de
   `villaplaisance.fr` par celui du V8, on switch la DB, et on archive le V7.

## État après Phase 0

- Dossier local créé : `~/Documents/C.L.A.U.D.E/villaplaisance-v8/`
- Code copié depuis le snapshot du 27 avril (`villa-plaisance-2026-04-27/code/`).
- `.env` local créé avec credentials MAMP par défaut (`localhost:8889`, `root/root`).
- `.env.example` mis à jour avec `DB_PORT`.
- `Database.php` patché pour supporter `DB_PORT` (rétrocompatible : default = `3306`).
- `.gitignore` créé (exclut `.env`, `public/uploads/`, dump SQL, etc.).
- `git init` fait, premier commit prêt à être créé.

## Phase 0 — TERMINÉE le 2026-05-01

- [x] DB locale `villaplaisance_v8_dev` créée dans MAMP MySQL 8.0.44.
- [x] Dump importé (28 tables).
- [x] Serveur PHP local lancé sur `http://localhost:8767/`.
- [x] Home affichée correctement avec l'ancien design v7.

### Pièges MariaDB → MySQL rencontrés (à retenir)

Le dump `database/vp_v7.sql` est généré sur **MariaDB** (sur o2switch) et
contient deux incompatibilités avec MySQL 8 :

1. **Collation `utf8mb4_uca1400_ai_ci`** (spécifique MariaDB 10.10+, inconnue
   de MySQL). Solution : créer un dump dérivé avec `utf8mb4_unicode_ci`
   (compatible des deux côtés) :
   ```bash
   sed 's/utf8mb4_uca1400_ai_ci/utf8mb4_unicode_ci/g' \
     database/vp_v7.sql > database/vp_v7-mysql.sql
   ```

2. **Foreign keys avec noms en doublon** (`CONSTRAINT \`1\``). MariaDB
   tolère des FK avec le même nom dans deux tables différentes du même
   schéma, MySQL refuse. Solution : renommer la 2e occurrence
   (`fk_itinerary_steps_itinerary`).

Le dump `vp_v7-mysql.sql` est gitignoré (généré à la demande, pas de
duplication dans le repo).

### Lancement local — commande définitive

Le serveur PHP built-in route TOUTES les requêtes vers `index.php` quand
on lui passe ce fichier en router. Du coup les fichiers statiques
(CSS, JS, images) ne sont jamais servis. Solution : un `router.php` à
la racine qui retourne `false` pour les fichiers existants et délègue
le reste à `public/index.php`. Lancer avec `-t public` (DocRoot =
`public/`) pour que `return false` serve le fichier depuis le bon dossier.

```bash
cd ~/Documents/C.L.A.U.D.E/villaplaisance-v8
php -S localhost:8767 -t public router.php
```

DB :
```bash
MYSQL=/Applications/MAMP/Library/bin/mysql80/bin/mysql
$MYSQL -u root -proot -h 127.0.0.1 -P 8889 villaplaisance_v8_dev
```

## Phases suivantes (récap)

- **Phase 1** : créer repo GitHub `villaplaisance-v8`, premier push.
- **Phase 2** : sous-domaine + DB + clone sur o2switch, premier déploiement.
- **Phase 3+** : portage du design V2 dans les Views Front, page par page.

## Stratégie de portage (Phase 3+)

Ordre proposé :
1. **Layout** `Views/layouts/front.php` (header, footer, overlay menu, fontes).
2. **Home** `front/home.php`.
3. **Chambres d'hôtes** `front/chambres.php`.
4. **Villa** `front/villa.php`.
5. **Espaces extérieurs** `front/exterieurs.php`.
6. **Sur place** `front/surplace.php`.
7. **Itinéraire** `front/itinerary.php` (liste + détail + commentaires).
8. **Journal** `front/journal.php` (liste) + `front/article.php` (détail).
9. **Hôte** `front/hote.php`.
10. **Contact** `front/contact.php` (brancher sur `ContactController::submit`).
11. **Légal** : mentions, confidentialité, plan du site, 404.

À chaque page :
- Garder les variables PHP exposées par le Controller (`$seo`, `$lang`, `$pieces`, `$articles`…).
- Remplacer le HTML par le markup V2.
- Adapter le CSS pour ne pas casser l'admin (qui partage le layout `admin.php`, distinct).
